Framework used React + vite and tailwind css
Redux used for state management
Start the server by the command "npm run dev" in the terminal after installing the react vite app and writing the complete code with the same folder structure
